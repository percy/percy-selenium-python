{
  "name": "percy-python-selenium",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "peerDependencies": {
    "@percy/cli": "^1"
  },
  "scripts": {
    "prebuild": "rm -rf dist/ build/",
    "build": "python setup.py sdist bdist_wheel"
  },
  "devDependencies": {
    "@percy/cli": "^1.0.0-beta.8",
    "chromedriver": "^84.0.0"
  }
}
