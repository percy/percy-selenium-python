{
  "name": "percy-python-selenium",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@percy/agent": "~0"
  },
  "scripts": {
    "prebuild": "rm -rf dist/ build/",
    "build": "python setup.py sdist bdist_wheel"
  },
  "devDependencies": {
    "chromedriver": "^80.0.1"
  }
}
