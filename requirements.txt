# CI/dev test env only — setup.py keeps `selenium>=3` for end users. This caps
# the test env below Selenium 4.28: 4.28+ drops Python 3.8, and on 3.9/3.10 its
# newer webdriver/urllib3 connection handling wedges geckodriver on the
# responsive test in CI (hung at 4.36.0). 4.27.x is the last line proven clean
# across 3.8-3.10; revisit once that wedge is root-caused so CI can track
# current Selenium.
selenium>=4.46.0,<4.47
requests==2.*
